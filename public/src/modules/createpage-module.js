angular.module('createpage-module', [])
    .controller('createpage-controller', ['$scope', '$http', '$location', 'authService', function ($scope, $http, $location, authService) {
        console.log('CREATE PAGE CONTROLLER');
    }]);